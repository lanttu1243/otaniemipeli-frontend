import { BoardPlace, Place, Board} from "@/utils/types";

export default function AddPlaceToBoard(): JSX.Element {
  // this component takes user input to add a place to a board by posting a BoardPlace object
  // to the backend


}
